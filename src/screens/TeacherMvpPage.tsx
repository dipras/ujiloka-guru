export function TeacherMvpPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-6xl flex-col gap-6 px-5 py-8">
      <header>
        <p className="text-sm font-semibold uppercase tracking-wide text-primary">
          LIDM Teacher Web
        </p>
        <h1 className="mt-2 text-3xl font-bold text-ink">
          Workspace MVP Guru
        </h1>
        <p className="mt-2 max-w-2xl text-sm leading-6 text-muted">
          Vite React, Tailwind, dan React Router siap untuk alur Generate QR,
          Scan Hasil, Scoring Web, dan Export CSV.
        </p>
      </header>
      <section className="card">
        <p className="text-sm text-muted">Tahap berikutnya: kontrak schema MVP.</p>
      </section>
    </main>
  );
}
